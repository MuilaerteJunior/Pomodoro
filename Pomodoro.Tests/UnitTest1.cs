using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Pomodoro;
using System.Threading.Tasks;
using Moq;

namespace Pomodoro.Tests {
    [TestClass]
    public class UnitTest1 {
        //[TestMethod]
        //public async Task TestMethod1() {
        //    var moq = new Mock<IBeepProvider>();
        //    var pdc = new PomodoroCore(moq.Object);
        //    //await pdc.StartCountdownTask(1, () => { });
        //    Assert.AreEqual(60, pdc.totalCount);
        //}
    }
}
